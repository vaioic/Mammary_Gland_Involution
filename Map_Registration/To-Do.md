# Troubleshooting

Check if the top-middle reference point is calculated accurately - looks correctly caclulated, weird alignments with top-middle could be caused by how the bounding box is determined, not as easy to fix
Check implementation of the transformation matrix on the tile centroids - added in the offset needed for correct application of transformation