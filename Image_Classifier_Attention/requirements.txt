# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Whole slide image processing
openslide-python>=1.2.0
opencv-python>=4.7.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0
h5py>=3.8.0
scikit-learn>=1.2.0

# Image augmentation
albumentations>=1.3.0

# Progress bars and utilities
tqdm>=4.65.0

# Experiment tracking (optional but recommended)
wandb>=0.15.0

# Visualization
matplotlib>=3.7.0

# For distributed training
# These are included with torch:
# - torch.distributed
# - torch.nn.parallel.DistributedDataParallel

# System dependencies (install separately):
# OpenSlide library must be installed at system level
#   Ubuntu/Debian: sudo apt-get install openslide-tools
#   macOS: brew install openslide
#   HPC: module load openslide (or equivalent)
