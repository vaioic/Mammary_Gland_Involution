# WSI Classification Pipeline - Requirements with GPU Support

# ============================================================================
# IMPORTANT: GPU Setup Required for Production Use
# ============================================================================
# The default 'pip install -r requirements.txt' installs CPU-only PyTorch.
# For GPU acceleration (HIGHLY RECOMMENDED), see GPU_SETUP_GUIDE.md
#
# Quick GPU Setup:
# 1. Check CUDA version: nvcc --version
# 2. Install GPU PyTorch:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# 3. Then install: pip install -r requirements.txt
# ============================================================================

# Core deep learning
# NOTE: These will install CPU-only versions by default
# For GPU: Install PyTorch with CUDA FIRST (see above), then run this file
torch>=2.0.0
torchvision>=0.15.0

# Whole slide image processing
openslide-python>=1.2.0
opencv-python>=4.7.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0
h5py>=3.8.0
scikit-learn>=1.2.0

# Image augmentation
albumentations>=1.3.0

# Progress bars and utilities
tqdm>=4.65.0

# Experiment tracking (optional but recommended)
wandb>=0.15.0

# Visualization
matplotlib>=3.7.0

# ============================================================================
# GPU-Specific PyTorch Installation (DO THIS FIRST)
# ============================================================================
#
# For CUDA 11.8 (recommended):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For CUDA 12.1:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For CUDA 11.7:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu117
#
# Then install this file:
# pip install -r requirements.txt
#
# Verify GPU access:
# python verify_gpu.py
# ============================================================================

# ============================================================================
# System Dependencies (install separately)
# ============================================================================
#
# 1. OpenSlide library (required for WSI processing):
#    Ubuntu/Debian: sudo apt-get install openslide-tools
#    macOS: brew install openslide
#    HPC: module load openslide
#
# 2. CUDA Toolkit (required for GPU):
#    Download from: https://developer.nvidia.com/cuda-downloads
#    Or HPC: module load cuda/11.8
#
# 3. NVIDIA Drivers (required for GPU):
#    Check: nvidia-smi
#    Install: https://www.nvidia.com/Download/index.aspx
# ============================================================================
